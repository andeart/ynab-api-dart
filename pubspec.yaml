name: ynab_api_dart
description: A Dart CLI for working with the YNAB API.
version: 0.1.0
repository: https://github.com/andeart/ynab-api-dart

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  http: ^1.6.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
